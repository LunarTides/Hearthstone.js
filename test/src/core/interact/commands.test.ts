import { expect, describe, test } from 'bun:test';
// eslint-disable-next-line no-unused-vars
import { commands, debugCommands } from '@Game/internal.js';

/*
 * Need to create a game in case the functions need it
 * This is a pretty big performance hit.
 */
// createGame();

describe('src/core/interact/commands', () => {
    test.todo('end', async () => {
        expect(false).toEqual(true);
    });

    test.todo('hero power', async () => {
        expect(false).toEqual(true);
    });

    test.todo('attack', async () => {
        expect(false).toEqual(true);
    });

    test.todo('use', async () => {
        expect(false).toEqual(true);
    });

    test.todo('titan', async () => {
        expect(false).toEqual(true);
    });

    test.todo('help', async () => {
        expect(false).toEqual(true);
    });

    test.todo('view', async () => {
        expect(false).toEqual(true);
    });

    test.todo('detail', async () => {
        expect(false).toEqual(true);
    });

    test.todo('concede', async () => {
        expect(false).toEqual(true);
    });

    test.todo('license', async () => {
        expect(false).toEqual(true);
    });

    test.todo('version', async () => {
        expect(false).toEqual(true);
    });

    test.todo('history', async () => {
        expect(false).toEqual(true);
    });

    test.todo('debug:give', async () => {
        expect(false).toEqual(true);
    });

    test.todo('debug:exit', async () => {
        expect(false).toEqual(true);
    });

    test.todo('debug:eval', async () => {
        expect(false).toEqual(true);
    });

    test.todo('debug:rl', async () => {
        expect(false).toEqual(true);
    });

    test.todo('debug:undo', async () => {
        expect(false).toEqual(true);
    });

    test.todo('debug:ai', async () => {
        expect(false).toEqual(true);
    });

    test.todo('debug:history', async () => {
        expect(false).toEqual(true);
    });
});
