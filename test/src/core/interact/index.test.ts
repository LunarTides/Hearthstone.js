import { describe, expect, test } from "bun:test";
import { interact } from "@Game/internal.js";

/*
 * Need to create a game in case the functions need it
 * This is a pretty big performance hit.
 */
// createGame();

describe("src/core/interact/index", () => {
	test.todo("deckCode", async () => {
		expect(false).toEqual(true);
	});

	test.todo("chooseOne", async () => {
		expect(false).toEqual(true);
	});

	test.todo("question", async () => {
		expect(false).toEqual(true);
	});

	test.todo("yesNoQuestion", async () => {
		expect(false).toEqual(true);
	});

	test.todo("selectPlayerTarget", async () => {
		expect(false).toEqual(true);
	});

	test.todo("selectCardTarget", async () => {
		expect(false).toEqual(true);
	});

	test.todo("shouldExit", async () => {
		expect(false).toEqual(true);
	});

	test.todo("verifyDiySolution", async () => {
		expect(false).toEqual(true);
	});

	test.todo("parseEvalArgs", async () => {
		expect(false).toEqual(true);
	});

	test.todo("cls", async () => {
		expect(false).toEqual(true);
	});
});
