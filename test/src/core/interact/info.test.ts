import { expect, describe, test } from 'bun:test';
// eslint-disable-next-line no-unused-vars
import { infoInteract } from '@Game/internal.js';

/*
 * Need to create a game in case the functions need it
 * This is a pretty big performance hit.
 */
// createGame();

describe('src/core/interact/info', () => {
    test.todo('watermark', async () => {
        expect(false).toEqual(true);
    });

    test.todo('license', async () => {
        expect(false).toEqual(true);
    });

    test.todo('withStatus', async () => {
        expect(false).toEqual(true);
    });

    test.todo('showGame', async () => {
        expect(false).toEqual(true);
    });

    test.todo('printPlayerStats', async () => {
        expect(false).toEqual(true);
    });

    test.todo('printBoard', async () => {
        expect(false).toEqual(true);
    });

    test.todo('printHand', async () => {
        expect(false).toEqual(true);
    });
});
