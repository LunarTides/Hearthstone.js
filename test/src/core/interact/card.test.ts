import { expect, describe, test } from 'bun:test';
// eslint-disable-next-line no-unused-vars
import { cardInteract } from '@Game/internal.js';

/*
 * Need to create a game in case the functions need it
 * This is a pretty big performance hit.
 */
// createGame();

describe('src/core/interact/card', () => {
    test.todo('useLocation', async () => {
        expect(false).toEqual(true);
    });

    test.todo('mulligan', async () => {
        expect(false).toEqual(true);
    });

    test.todo('dredge', async () => {
        expect(false).toEqual(true);
    });

    test.todo('discover', async () => {
        expect(false).toEqual(true);
    });

    test.todo('doPlaceholders', async () => {
        expect(false).toEqual(true);
    });

    test.todo('getReadable', async () => {
        expect(false).toEqual(true);
    });

    test.todo('view', async () => {
        expect(false).toEqual(true);
    });

    test.todo('spawnInDiyCard', async () => {
        expect(false).toEqual(true);
    });
});
