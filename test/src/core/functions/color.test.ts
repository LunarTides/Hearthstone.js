import { expect, describe, test } from 'bun:test';
// eslint-disable-next-line no-unused-vars
import { colorFunctions } from '@Game/internal.js';

/*
 * Need to create a game in case the functions need it
 * This is a pretty big performance hit.
 */
// createGame();

describe('src/core/functions/color', () => {
    test.todo('fromRarity', async () => {
        expect(false).toEqual(true);
    });

    test.todo('fromTags', async () => {
        expect(false).toEqual(true);
    });

    test.todo('if', async () => {
        expect(false).toEqual(true);
    });

    test.todo('stripTags', async () => {
        expect(false).toEqual(true);
    });

    test.todo('stripColors', async () => {
        expect(false).toEqual(true);
    });

    test.todo('stripAll', async () => {
        expect(false).toEqual(true);
    });
});
