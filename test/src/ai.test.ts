import { describe, expect, test } from "bun:test";
import { Ai } from "@Game/ai.ts";

describe("src/ai", () => {
	test.todo("calcMove", async () => {
		expect(false).toEqual(true);
	});

	test.todo("attack", async () => {
		expect(false).toEqual(true);
	});

	test.todo("legacyAttack1", async () => {
		expect(false).toEqual(true);
	});

	test.todo("promptTarget", async () => {
		expect(false).toEqual(true);
	});

	test.todo("discover", async () => {
		expect(false).toEqual(true);
	});

	test.todo("dredge", async () => {
		expect(false).toEqual(true);
	});

	test.todo("chooseOne", async () => {
		expect(false).toEqual(true);
	});

	test.todo("question", async () => {
		expect(false).toEqual(true);
	});

	test.todo("yesNoQuestion", async () => {
		expect(false).toEqual(true);
	});

	test.todo("trade", async () => {
		expect(false).toEqual(true);
	});

	test.todo("forge", async () => {
		expect(false).toEqual(true);
	});

	test.todo("mulligan", async () => {
		expect(false).toEqual(true);
	});

	test.todo("analyzePositive", async () => {
		expect(false).toEqual(true);
	});

	test.todo("analyzePositiveCard", async () => {
		expect(false).toEqual(true);
	});
});
